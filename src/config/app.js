import {API_BASE_URL, API_ASSET_URL, GOOGLE_MAPS_APIKEY as MAP_KEY} from '@env';

export const asset_url = API_ASSET_URL;
export const api_base_url = API_BASE_URL;
export const preloadedStoreData = false;
export const currencySymbol = '৳';

export const GOOGLE_MAPS_APIKEY = MAP_KEY;

export default {
    asset_url,
    api_base_url,
    GOOGLE_MAPS_APIKEY,
    preloadedStoreData,
    currencySymbol,
};
